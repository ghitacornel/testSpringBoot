create table person
(
    id   int primary key,
    name varchar(50),
    pass varchar(50)
);

create table person_jdbc
(
    id   int primary key,
    name varchar(50),
    pass varchar(50)
);